import {} from /* pgTable, uuid, timestamp */ 'drizzle-orm/pg-core';

// export const LudoGame = pgTable('LudoGame', {
// 	id: uuid('id').primaryKey().defaultRandom(),
// 	createdAt: timestamp('createdAt', { precision: 0 }).notNull().defaultNow()
// });

// export type LudoGame = typeof LudoGame.$inferSelect;
